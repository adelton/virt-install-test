#!/usr/bin/expect
set DOMAIN [lindex $argv 0]

set timeout 500

spawn -noecho virsh -e "^D" console "$DOMAIN"
expect {
	"Connected to domain" { send \r ; exp_continue; }
	"login:" { send \x04; puts \n; }
}
